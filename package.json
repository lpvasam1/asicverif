{
  "name": "asicverif",
  "version": "0.1.0",
  "description": "UVM testbench generator — built by a DV engineer, for DV engineers",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy ."
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
